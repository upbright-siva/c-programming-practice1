#include <stdio.h>

int main() {
    int score = 5;

    // Use Control Statement - If Else Statement
    if (score > 10) {
        // If the condition true, run below code
        printf("Passed");
    } else {
        // If the condition False, run below code
        printf("Try Again");
    }
}