#include <stdio.h>

int main() {
    /**
     * Operators
     * `````````
     * 1. Arithmetic
     * 2. Comparison
     * 3. Logical Operator
     */

     /**
      * Arithmetic operators
      * 1. Addition
      * 2. Substraction
      * 3. Division
      * 4. Multiplication
      * 5. Modulus
      */
     
      /**
       * 1. Addition
       * a + b = c
       */
      // Declare variables
      int a = 6546654;
      int b = 5749879;
      int c;

      // Addition
      c = a + b;

      // Display the values
      printf("%d + %d = %d", a, b, c);

    return 0;
}